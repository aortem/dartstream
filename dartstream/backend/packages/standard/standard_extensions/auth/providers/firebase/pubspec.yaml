name: ds_firebase_auth_provider
description: Firebase authentication provider for Dartstream.
version: 0.0.2
repository: https://github.com/aortem/dartstream

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  firebase_dart_admin_auth_sdk: ^0.0.9
  ds_auth_base: ^0.0.2
  ds_standard_features: ^0.1.6

dev_dependencies:
  test: ^1.29.0
  ds_tools_testing: ^0.1.4
