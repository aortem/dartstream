name: firebase_auth_example
description: Firebase Authentication example for DartStream
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # HTTP Server
  shelf: ^1.4.0

dev_dependencies:
  lints: ^2.0.0
