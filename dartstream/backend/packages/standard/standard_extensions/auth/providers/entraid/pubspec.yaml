name: ds_entraid_auth_provider
description: Azure authentication provider for Dartstream.
version: 0.0.1-pre
repository: https://github.com/aortem/dartstream

environment:
  sdk: ^3.10.0

dependencies:
  # Add the base auth package as a dependency
  ds_auth_base: ^0.0.1

dev_dependencies:
  test: ^1.25.0
