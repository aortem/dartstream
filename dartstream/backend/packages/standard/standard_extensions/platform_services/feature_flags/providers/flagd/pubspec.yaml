name: ds_flagd_provider
description: flagd feature flag provider for DartStream.
version: 0.0.1-pre+1
repository: https://github.com/aortem/dartstream

publish_to: none # Mark the package as non-publishable for local testing:

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  http: ^1.2.2
  ds_feature_flags_base:
    path: ../../base
  ds_standard_features: ^0.1.7

dev_dependencies:
  ds_tools_testing: ^0.1.4
