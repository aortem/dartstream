#This pubspec file imports is used for any authentication framework features.
#This works for both mobile and web.
name: ds_feature_flags_base
description: Base feature flag interfaces and manager contracts for DartStream.
version: 0.0.1
repository: https://github.com/aortem/dartstream

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  openfeature_dart_server_sdk: ^0.0.16
  ds_standard_features: ^0.1.6
