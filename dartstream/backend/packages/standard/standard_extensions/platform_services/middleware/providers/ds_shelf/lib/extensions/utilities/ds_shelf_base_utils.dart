// lib/src/utilities/ds_shelf_base_utils.dart

/// Base utility class for shared behavior (if needed).
/// You can remove if not used directly.
class DsShelfBaseUtils {
  /// Centralized logging hook for framework utilities
  void log(String message) {
    print('[ds_shelf_utils] $message');
  }
}
