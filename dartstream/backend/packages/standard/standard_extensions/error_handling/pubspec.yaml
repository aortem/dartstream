name: ds_error_handling
description: A standalone error handling package for Dartstream that provides middleware for catching exceptions and returning structured JSON responses.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.0

dev_dependencies:
  shelf_router: ^1.1.0
  shelf_static: ^1.1.0
  test: ^1.24.0
