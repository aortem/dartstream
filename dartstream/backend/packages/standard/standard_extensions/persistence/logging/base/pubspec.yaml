name: ds_logging_base
description: Base logging interfaces and manager for DartStream.
version: "0.0.1"
repository: https://github.com/aortem/dartstream

publish_to: none # Mark the package as non-publishable for local testing:

environment:
 sdk: ^3.10.8

resolution: workspace

entry_point: "lib/ds_logging_base_export.dart"
