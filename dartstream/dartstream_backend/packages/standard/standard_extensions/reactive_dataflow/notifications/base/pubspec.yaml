name: ds_notifications_base
description: "Base package for DartStream notifications, including foundational classes and interfaces.."
version: "0.0.1-pre"
repository: https://github.com/aortem/dartstream

publish_to: none # Mark the package as non-publishable for local testing:

environment:
 sdk: ^3.8.1

resolution: workspace

entry_point: "lib/amazon_auth_provider.dart"