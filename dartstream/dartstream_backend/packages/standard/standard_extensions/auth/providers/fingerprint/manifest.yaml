name: ds_fingerprint_auth_provider
version: "0.0.1-pre"
description: "fingerprint authentication provider for Dartstream."
dependencies:
  - "Auth >=0.0.1"
entry_point: "lib/ds_fingerprint_auth_provider.dart"