# This pubspec file imports dependencies for standard web Dartstream features.
# For users that only want to use web and NOT mobile. Otherwise, use ds_standard_features.

name: ds_standard_features_web
description: "⚠️ Deprecated: This package is no longer maintained. Please use ds_standard_features as the alternative package: https://pub.dev/packages/ds_standard_features."
version: 0.0.1-pre+2
repository: https://github.com/dartstream

environment:
  sdk: ">=3.4.0 <4.0.0"

# Dependencies are added in sections alphabetically and according to features.
dependencies:

  # STANDARD PACKAGES - Built By The Dart Team
  matcher: ^0.12.16+1
  web: ^1.0.0

# Globally Activated Package
# executables:
#   ds: ds_cli
