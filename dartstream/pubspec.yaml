# Purpose:
  # This is the root-level pubspec for the entire monorepo.
  # Acts as an entry point or workspace root for tools like melos.
  # Does not include actual executable or package code.
  # Could define repository-wide metadata, global dev dependencies, or scripts.

name: ds_workspace
version: 0.0.1
description: A package for DartStream project-wide configurations and scripts.
repository: https://github.com/aortem/dartstream

environment:
  sdk: ^3.8.1

# This pubspec file is used for any dependencies that both the frontend and backend framework use(aka. shared dependencies)
# Dependencies are added in sections alphabetically and according to features.
dependencies:

# Dev dependencies are added in sections alphabetically and according to features.
dev_dependencies:
