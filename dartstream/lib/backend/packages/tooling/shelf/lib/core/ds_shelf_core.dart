// lib/core/core.dart
part of '../ds_shelf.dart';

class CoreShelf {
  void setupServer() {
    var handler = initializeServer();

    // Additional server setup and middleware configuration
  }
}
